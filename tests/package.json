{
  "name": "nso-portal-tests",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Security & logic tests for the NSO Portal (Firestore rules + shared utils).",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.0.0",
    "vitest": "^3.0.5"
  }
}
